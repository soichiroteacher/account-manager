# アカウント管理アプリ 引き継ぎメモ(後任の開発者向け)

最終更新: 2026-09-27

使う先生向けの説明は [README.md](README.md)、変更の記録は [CHANGELOG.md](CHANGELOG.md)、全アプリ共通のルールは `../CLAUDE.md` にある。

## 1. このアプリについて

- 中学校で、生徒のGoogleアカウントや学習サービスのアカウント、タブレット端末をまとめて管理するためのアプリ。
- 作成・更新するのは教務担当・ICT担当。名簿はほかの先生も閲覧・編集する。
- 年度初めなどに、生徒一人ひとりへ「アカウントシート」(B5・1人1枚)を配る。
- GitHub: https://github.com/soichiroteacher/account-manager (Public)
- appcopy(Googleドライブ同期): `C:\Users\idolo\Documents\projects\appcopy\account-manager\`

## 2. 運用のしかた(ユーザーと確定済み)

- 校務共有サーバーの同じフォルダに `index.html` 一式とデータファイル `アカウント名簿.json`(以前の名前は `アカウント名簿.dat`)を置き、全員がそこから開く。
- 閲覧用の別アプリ・別ファイルは作らない(ユーザーの指示:「わけわからなくなるので必要ない」)。
- パスコードはない(2026-09-27 に廃止)。開くとまず「閲覧のみ」で、「編集する」を押せば誰でも編集できる。見られる人の制限は、共有サーバーのフォルダの権限で行う。
- 同時編集は「行事予定アプリ」と同じ考え方で防ぐ(編集中の記録をファイルに書き、10分操作がなければ期限切れ)。
- ブラウザは Chrome / Edge が前提。データファイルへの直接保存に File System Access API を使うため。

## 3. ファイル構成

| ファイル | 内容 |
|---|---|
| `index.html` | 画面(開始画面・パスコード画面・本体・各ダイアログ) |
| `app.js` | アプリ本体(約2300行、IIFE 1つ) |
| `crypto.js` | 以前の形式(暗号化)のファイルを読むための復号処理(`window.AppCrypto`)。**消さないこと** |
| `style.css` | スタイル。アカウントシートのB5レイアウトもここ |
| `lib/` | jsPDF 2.5.1 / html2canvas 1.4.1 / SheetJS 0.18.5(オフラインで動くようローカルに同梱) |
| `scripts/serve.ps1` | 開発用の簡易Webサーバー(PowerShell、ポート8080) |
| `.claude/launch.json` | 上記サーバーをプレビューで起動する設定 |

## 4. データと保存の仕組み

- **データファイル**は JSON。形式は2種類あり、`version` で見分ける。
  - **version 2(現在)**: `{ format, version: 2, savedAt, editLock, students, settings }`。暗号化しない。拡張子は `.json`(以前から使っているファイルは `.dat` のままでも読める)。
  - **version 1(〜2026-09-26)**: `students` と `settings` をパスコードで暗号化し、`kdf` / `iter` / `salt` / `iv` / `data` に入れていた(PBKDF2-SHA256 60万回 + AES-256-GCM)。
  - version 1 を開くと、パスコード入力画面を出して復号する(`isEncryptedDoc` / `decodeDoc`)。「編集する」で保存した時点で version 2 に書き換わる。
  - 共通ルールで「データの暗号化は原則しない」と決めたため、2026-09-27 に version 2 へ移行した。**古いファイルやバックアップを開けるよう、復号の処理(`crypto.js`)は残しておくこと。**
  - `settings` にはシートのレイアウトと文言、パスワード生成ルール、Google CSVの設定、自動で編集を終えるまでの時間が入る。
- **バックアップと復元**: 「データを書き出す(バックアップ)」で `アカウント管理_バックアップ_日付.json`(version 2 と同じ形)を保存。「データを読み込む(復元)」は編集中のみ使え、確認のうえで今のデータを置き換えて保存する。version 1 のバックアップは直接復元せず、いったんデータファイルとして開いて変換してもらう。
- **編集の記録(`editLock`)**: `{ active, since, by, session }`。
  - 「編集する」を押すと、ファイルを読み直して最新の内容で編集を始め、編集の記録を書き込む。
  - 保存するたびに、ほかのセッションの有効な記録がないか確かめる。あれば保存を止め、閲覧モードに戻して最新の内容を読み直す。
  - 「編集を終える」で `active:false` にする。
- **このPCだけに保存するもの**:
  - localStorage: 編集者名(`accountManagerApp.editorName`)
  - IndexedDB: 前回開いたファイルのハンドル(`accountManagerApp` DB の `kv` ストア、キー `dataFileHandle`)
- **旧バージョンからの引き継ぎ**: 旧版は localStorage に保存していた(キーは `LEGACY_KEYS`)。データファイルを新しく作るときに取り込むことができ、取り込んだ後は localStorage から消す。
- **公開リポジトリへの流出防止**: `.gitignore` で `*.dat` `*.xlsx` `*.xls` `*.csv` `*.json`(`launch.json` は除く)と `*.pdf` を除外している。

## 5. 実装済みの機能

- 生徒の追加・編集・削除
  - 学籍番号・在籍状況・Googleアカウントを持つ
  - その他のサービスは項目を自由に追加できる
  - タブレット端末(番号・シリアル・状態・代替機・メモ)
- 一覧
  - 検索、表示の切り替え(在籍中/転出・卒業/すべて/端末: 修理中/端末: 未配布/端末: 転出・卒業で未返却)
  - クラスは数字順に並べる
- Excel
  - 書き出し・読み込み・テンプレート。見出しから列を自動で判別し、セルは文字列形式にする
  - 読み込みは「追加・更新」と「すべて置き換え」を選べる。照合は学籍番号が優先で、なければ取り込み前の「クラス＋出席番号」を使う
  - 学籍番号の重複は保存させない。座席(クラス＋出席番号)と端末番号の重複は警告する
- アカウントシート(PDF)
  - JIS B5・1人1枚。学校名・タイトル・全員共通の記載事項・個人情報の注意文を設定でき、プレビューで確認できる
  - 項目の表示/非表示と並び順を変えられる。IDとパスワードは等幅フォント
  - html2canvasで画像にしてPDFに貼るため、画面に表示できる字は外字も含めてそのまま出る
- 独自外字(Unicodeの私用領域の文字)の検出
  - 一覧に「外字あり」の印と絞り込み、編集中の警告、Excel取り込み時の一覧、PDF出力前の確認
- パスワード
  - 自動生成(暗号用の乱数を使い、紛らわしい文字を除ける)と一括生成
  - 画面では伏せ字にし、「表示」を押したときだけ見える
- パスワード再発行
  - 対象アカウントを選んで新しいパスワードを記録し、履歴(日付・対象・メモ)を残す。古いパスワードは残さない
  - 「(再発行)」と入ったシートを1枚出力できる
- 年度更新
  - 最上学年を卒業にし、ほかの学年のクラス名の「○年」を1つ上げる
  - クラス替えは、Excelで学籍番号を使って取り込む
- Google管理コンソール用CSV
  - 姓・名・メール・パスワード・組織部門(`{学年}`/`{クラス}` の置き換えに対応)・Employee ID(学籍番号)・初回ログイン時にパスワード変更
- 共有ファイルの運用
  - 閲覧/編集モード、自動保存、ほかの人の編集中表示、1分ごとの更新確認
  - 「データを書き出す(バックアップ)」「データを読み込む(復元)」、設定(編集者名・自動で編集を終えるまでの時間・別ファイルを開く)
  - 編集中に一定時間操作がないと、自動で保存して編集を終える(5/10/15/30分)
  - 以前の形式(パスコード付き)のファイルを開いたときだけ、パスコード入力画面が出る

## 6. 未確認・要確認のこと(実機でしか確かめられないもの)

- [ ] **実際の共有フォルダ上の、今使っているパスコード付きの `.dat` を新しい形式に変換できるか**(2026-09-27 の変更。テストではブラウザ内の仮のファイルで、変換・バックアップ・復元・新規作成まで確認済み)。変換の前に、共有サーバー上で `.dat` を別名コピーしておくこと。
- [ ] **実際の共有フォルダ(ネットワークドライブ・UNCパス)上のデータファイルでの動作**。開発中はブラウザ内の仮のファイル(OPFS)で代わりに試した。2台のPCで開き、編集中の表示・警告・保存が止まることを確認する。
- [ ] `file://` で開いたときに IndexedDB と File System Access API が使えるか(前回のファイルを自動で開けるか)。
- [ ] 閲覧モードでファイルを開いた先生が「編集する」を押したときの、書き込み許可の確認ダイアログの出方。
- [ ] 自動で編集を終える機能(最短5分)の実際の動作。テストでは時間の都合で確認していない。
- [ ] Google管理コンソールへのCSVアップロード。列名は一般的な形式に合わせた。まず数人分で試す。
- [ ] ファイルとして実際にダウンロード・保存されるか(PDF・Excel・CSV・バックアップ)。プレビュー環境では生成までしか確認していない。
- [ ] B5での印刷の仕上がり(用紙サイズB5・実際のサイズ100%)。

## 7. 次にやることの候補

- 画面上部のボタンが多い(編集モードで12個)ので、グループ分けやメニュー化で整理する。以前に提案済みで、未着手。
- 行事予定アプリのように、アプリ内に「使い方(ヘルプ)」画面を作る。
- 意図的に実装していない提案(ユーザーの判断で除外):
  - 2. 担任用のクラス名簿の出力
  - 9. 配布・初回ログインの確認チェック
  - 10. クラスの絞り込みと選択した生徒だけのPDF出力
  - 12. Googleスプレッドシートへの移行
- PDFの文字が画像のため選択できない(ファイルサイズも大きくなる)。改善はできるが手間が大きいため保留中。
- 実機テストで問題が出たら、6の項目から直す。

## 8. 開発メモ

- **コードのコメント**: 共通ルールでは日本語で書くことになっているが、`app.js` には英語のコメントが多く残っている。触った箇所から日本語に直していく。
- **動作確認**: `.claude/launch.json` の `account-manager-static`(`scripts/serve.ps1`、http://localhost:8080)でプレビューを起動する。`file://` のプレビューでは静的なスナップショットになり、JSが動かない。
- **ファイル操作のテスト**: `window.showSaveFilePicker` / `showOpenFilePicker` を、OPFS(`navigator.storage.getDirectory()`)のハンドルを返すものに差し替えて試した。タブを2つ開くと、別々のPC(別セッション)を想定して同時編集を試せる。
- **プレビューのブラウザでは `alert` / `confirm` が自動で閉じる**(`confirm` は false を返す)。テストでは `window.confirm` を差し替えて使う。
- **グローバル指示(`~/.claude/CLAUDE.md`)**: アプリのファイルを変更したら、`.git` を除いてappcopyへ丸ごとミラーコピーし、そのたびに add → commit → push する。force push はしない。
- **gh CLI** は `C:\Program Files\GitHub CLI\gh.exe`。認証済み(soichiroteacher)。
- 外字の判定に使う正規表現は、エスケープ表記(`\u{E000}` など)で書くこと。私用領域の文字をそのまま書くと、目に見えなくなる。
