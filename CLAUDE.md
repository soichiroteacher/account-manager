# アカウント管理アプリ(account-manager)

- 全アプリ共通のルールは `../CLAUDE.md`。必ず従うこと。
- 作業を始める前に [HANDOFF.md](HANDOFF.md) を読むこと(仕組み・注意点・次にやること)。
- 機能を変えたら README.md(先生向け)・CHANGELOG.md・HANDOFF.md を同時に更新する。
- `crypto.js` は以前の形式(暗号化)のデータファイルを読むために必要。消さないこと。
- 生徒の実データは、このフォルダに置かない(GitHub と Google ドライブに送られるため)。
